apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: '4'
  labels:
    app: wotao-sub-client-changsha
  name: wotao-sub-client-changsha
  namespace: wt-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: wotao-sub-client-changsha
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: wotao-sub-client-changsha
    spec:
      containers:
        - env:
            - name: NODE_ENV
              value: production
            - name: HOST
              value: 0.0.0.0
          image: ${IMAGE}
          imagePullPolicy: IfNotPresent
          name: wotao-sub-client-changsha
          ports:
            - containerPort: 3353
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
          terminationMessagePath: /dev/termination-log
          terminationMessagePolicy: File
          volumeMounts:
            - mountPath: /etc/localtime
              name: volume-localtime
      dnsPolicy: ClusterFirst
      imagePullSecrets:
        - name: wotao
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      volumes:
        - hostPath:
            path: /etc/localtime
            type: ''
          name: volume-localtime