<!--components/search/putunder/putunder.wxml-->
<view class="scroll-item" wx:for="{{putunderList}}" wx:key="index" bindtap="changeNew" data-type="{{item}}">
  <text class="{{newIndex == item.type? 'active-scroll-sty':''}}">{{item.title}}</text>
</view>
